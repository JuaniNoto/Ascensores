<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.3/css/all.css" integrity="sha384-SZXxX4whJ79/gErwcOYf+zWLeJdY/qpuqC4cAa9rOGUstPomtqpuNWT9wdPEn2fk" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Document</title>
</head>
<body>
    
    <%- include('partials/headerIndex.ejs') %>

    <main class="login">
        <div class="caja">
           <!-- <div class="boton">
                <a href="" class="ingresar">Ingresar</a>
            </div> -->
            <div class="formulario">
                <h3>Ingresar</h3>
                <form action="/login" method="POST" id="form-register">
                    <div class="form-group" id="email-group">
                        <input type="email" placeholder="Email" name="email" id="email" value="<%= (typeof old != 'undefined')? old.email:'' %>" >    
                        <% if(typeof errors != 'undefined'){ %>
                            <% if(typeof errors.email != 'undefined'){ %>
                                <small id="small-email"><%= errors.email.msg %></small>
                            <% } %>
                        <% } %>                   
                    </div>
                    
                    <div class="form-group" id="password-group">
                        <div class="password-input">
                            <span class="eyeIcon hide"></span>
                            <input type="password" class="password" id="password" placeholder="Contraseña" name="password">
                        </div>
                        <% if(typeof noCoincidence != 'undefined'){ %>
                            <small><%= noCoincidence %></small>
                        <% } %>
                        <% if(typeof errors != 'undefined'){ %>
                            <% if(typeof errors.password != 'undefined'){ %>
                                <small id="small-passsword"><%= errors.password.msg %></small>
                            <% } %>
                        <% } %>
                    </div>
                    
                    <a href="#">Olvidaste tu contraseña?</a>
                    <button type="submit">Ingresar</button>
                </form>
            </div>
            <p>Todavía no te registraste? <a href="#">Regístrate acá</a></p>
        </div>
    </main>

    <script src="/js/loginValidation.js"></script>

</body>
</html>