<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.3/css/all.css" integrity="sha384-SZXxX4whJ79/gErwcOYf+zWLeJdY/qpuqC4cAa9rOGUstPomtqpuNWT9wdPEn2fk" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Document</title>
</head>
<body>

    <%- include('../partials/header.ejs') %>

    <main class="create-form">

        <h4>Crear Nuevo Cliente</h4>

        <form action="/admin/clientes/create" method="POST">

            <div class="form-group cuil-group">
                <label for="cuit">CUIT</label>
                <input type="number" name="cuit" placeholder="CUIT" value="<%=(typeof old == 'undefined') ? '':(typeof old.cuit != 'undefined') ? old.cuit:'' %>">
                <% if(typeof errors != 'undefined'){ %>
                    <% if(typeof errors.cuit != 'undefined'){ %>
                        <small><%= errors.cuit.msg %></small>
                    <% } %>
                <% } %>
            </div>
            <div class="form-group razon-group">
                <label for="razon">Razon Social</label>
                <input type="text" name="razon" placeholder="Razon Social" value="<%=(typeof old == 'undefined') ? '':(typeof old.razon != 'undefined') ? old.razon:'' %>">
                <% if(typeof errors != 'undefined'){ %>
                    <% if(typeof errors.razon != 'undefined'){ %>
                        <small><%= errors.razon.msg %></small>
                    <% } %>
                <% } %>
            </div>
            </div>    
            <div class="form-group direccion-group">
                <label for="direccion">Dirección</label>
                <input type="text" name="direccion" placeholder="Dirección" value="<%=(typeof old == 'undefined') ? '':(typeof old.direccion != 'undefined') ? old.direccion:'' %>">
            </div>
            <div class="form-group localidad-group">
                <label for="localidad">Localidad</label>
                <input type="text" name="localidad" placeholder="Localidad" value="<%=(typeof old == 'undefined') ? '':(typeof old.localidad != 'undefined') ? old.localidad:'' %>">
            </div>
            <div class="form-group codigopostal-group">
                <label for="codigopostal">Codigo Postal</label>
                <input type="number" name="codigopostal" placeholder="codigopostal" value="<%=(typeof old == 'undefined') ? '':(typeof old.codigopostal != 'undefined') ? old.codigopostal:'' %>">
            </div>
            <div class="form-group provincia-group">
                <label for="provincia">Provincia</label>
                <select name="provincia" id="provincia">
                <option value="" hidden>Seleccione Provincia</option>
                <option value="Buenos">Bs As</option>
                <option value="">Cordoba</option>
                <option value="">Sta Fe</option>
                </select>
            </div>
            <div class="form-group telefono-group">
                <label for="telefono">Teléfono</label>
                <input type="number" name="telefono" placeholder="Teléfono" value="<%=(typeof old == 'undefined') ? '':(typeof old.telefono != 'undefined') ? old.telefono:'' %>">
            </div>
            <div class="form-group mail-group">
                <label for="mail">Email</label>
                <input type="email" name="email" placeholder="Email" value="<%=(typeof old == 'undefined') ? '':(typeof old.email != 'undefined') ? old.email:'' %>">
                <% if(typeof errors != 'undefined'){ %>
                    <% if(typeof errors.email != 'undefined'){ %>
                        <small><%= errors.email.msg %></small>
                    <% } %>
                <% } %>
            </div>
            <div class="form-group admin-group">
                <label for="abono">Abono</label>
                <select name="abono" id="abono">
                    <option value="" hidden>-Seleccione el tipo de Abono-</option>
                    <% for(let i=0; i < abonos.length; i++){ %>
                    <option value="<%=abonos[i].id%>"><%=abonos[i].tipo%></option>
                    <%}%>
                </select>
                <% if(typeof errors != 'undefined'){ %>
                    <% if(typeof errors.abono != 'undefined'){ %>
                        <small><%= errors.abono.msg %></small>
                    <% } %>
                <% } %>
            </div>
            <div class="form-group button-group">
                <button type="submit">Crear</button>
            </div>

        </form>
    </main>

    <%- include('../partials/footer.ejs') %>

</body>
</html>