const express = require('express');
const router = express.Router();

const configController = require('../controller/configControllers');

/*router.get('/administraciones',configController.funcion);

router.get('/abonos',configController.funcion);

router.get('/tareas',configController.funcion);

router.get('/usuarios',configController.funcion);*/

module.exports = router;